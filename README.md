# 🔄 3X-UI → Pasarguard Migration Tool

> Полнофункциональный инструмент для миграции пользователей, настроек и статистики трафика из 3X-UI (X-UI) в Pasarguard. Поддерживает SQLite, PostgreSQL и MySQL/MariaDB для обеих систем.

**Основные возможности:**
- ✅ Миграция всех пользователей с сохранением UUID и паролей
- ✅ Перенос использованного трафика и лимитов
- ✅ Миграция сроков действия подписок
- ✅ Поддержка всех типов БД (SQLite, PostgreSQL, MySQL/MariaDB)
- ✅ Автоматическая генерация недостающих UUID и паролей
- ✅ Обновление существующих пользователей без потери данных

---

## Требования

1. Python 3.11+
2. python-dotenv
3. psycopg2-binary (для PostgreSQL)
4. pymysql (для MySQL/MariaDB)

---

## 🚀 Запуск миграции

### 1. Установка зависимостей

```sh
pip install -r requirements.txt
```

### 2. Подготовка файлов базы данных (для SQLite)

Если вы используете **SQLite** (файлы `.db` или `.sqlite3`), вам нужно скопировать файлы базы данных в папку с проектом:

**Для 3X-UI:**
- Найдите файл базы данных 3X-UI (обычно `x-ui.db` или `xui.db`)
- Скопируйте его в папку проекта и укажите путь в `.env` (например: `XUI_DB_PATH=x-ui.db`)
- Или укажите полный путь к файлу: `XUI_DB_PATH=/path/to/your/x-ui.db`

**Для Pasarguard:**
- Найдите файл базы данных Pasarguard (обычно `db.sqlite3`)
- Скопируйте его в папку проекта и укажите путь в `.env` (например: `PASAR_DB_PATH=db.sqlite3`)
- Или укажите полный путь к файлу: `PASAR_DB_PATH=/path/to/your/db.sqlite3`

📁 **Пример структуры папки проекта:**
```
migration/
├── migration.py
├── requirements.txt
├── .env
├── x-ui.db          ← файл БД 3X-UI (если используете SQLite)
└── db.sqlite3       ← файл БД Pasarguard (если используете SQLite)
```

💡 **Совет:** Вы можете использовать абсолютные пути в `.env`, если файлы находятся в других местах:
```env
XUI_DB_PATH=/home/user/3x-ui/data/x-ui.db
PASAR_DB_PATH=/opt/pasarguard/db.sqlite3
```

### 3. Настройка конфигурации

Создайте файл `.env` и настройте подключения к базам данных (см. раздел [Конфигурация](#⚙️-конфигурация))

### 4. Запуск миграции

```sh
python migration.py
```

---

## ⚙️ Конфигурация

В `.env` заполните переменные для подключения к базам данных:

### База данных 3X-UI (исходная)

```
## Тип БД: sqlite, postgresql, mysql, mariadb
XUI_DB_TYPE=sqlite

## Для SQLite
## Укажите имя файла (если файл в папке проекта) или полный путь к файлу
## Примеры:
## XUI_DB_PATH=x-ui.db                    ← файл в папке проекта
## XUI_DB_PATH=/home/user/x-ui.db          ← абсолютный путь (Linux/Mac)
## XUI_DB_PATH=C:\Users\user\x-ui.db      ← абсолютный путь (Windows)
XUI_DB_PATH=x-ui.db

## Для PostgreSQL/TimescaleDB (раскомментируйте при использовании)
# XUI_DB_HOST=localhost
# XUI_DB_PORT=5432
# XUI_DB_NAME=xui
# XUI_DB_USER=postgres
# XUI_DB_PASSWORD=your_password

## Для MySQL/MariaDB (раскомментируйте при использовании)
# XUI_DB_HOST=localhost
# XUI_DB_PORT=3306
# XUI_DB_NAME=xui
# XUI_DB_USER=root
# XUI_DB_PASSWORD=your_password
```

### База данных Pasarguard (целевая)

```
## Тип БД: sqlite, postgresql, mysql, mariadb
PASAR_DB_TYPE=sqlite

## Для SQLite
## Укажите имя файла (если файл в папке проекта) или полный путь к файлу
## Примеры:
## PASAR_DB_PATH=db.sqlite3                      ← файл в папке проекта
## PASAR_DB_PATH=/opt/pasarguard/db.sqlite3      ← абсолютный путь (Linux/Mac)
## PASAR_DB_PATH=C:\pasarguard\db.sqlite3        ← абсолютный путь (Windows)
PASAR_DB_PATH=db.sqlite3

## Для PostgreSQL/TimescaleDB (раскомментируйте при использовании)
# PASAR_DB_HOST=localhost
# PASAR_DB_PORT=5432
# PASAR_DB_NAME=pasarguard
# PASAR_DB_USER=postgres
# PASAR_DB_PASSWORD=your_password

## Для MySQL/MariaDB (раскомментируйте при использовании)
# PASAR_DB_HOST=localhost
# PASAR_DB_PORT=3306
# PASAR_DB_NAME=pasarguard
# PASAR_DB_USER=root
# PASAR_DB_PASSWORD=your_password
```

⚠️ **Никогда не публикуйте свои пароли и данные для подключения к БД!**

---

## 🛠️ Что делает миграция

Скрипт выполняет полную миграцию всех пользователей из 3X-UI в Pasarguard:

1. **Извлекает данные** из базы 3X-UI:
   - Все inbounds и их настройки
   - Клиенты из каждого inbound
   - Статистику трафика (up, down, total)
   - Срок действия (expiry_time)
   - Лимиты трафика

2. **Импортирует в Pasarguard**:
   - Создает новых пользователей или обновляет существующих
   - Генерирует UUID для VMess/VLESS
   - Генерирует безопасные пароли для ShadowSocks и Trojan
   - Сохраняет использованный трафик
   - Переносит лимиты трафика и сроки действия
   - Привязывает пользователей к группам (если есть)

3. **Автоматическая обработка**:
   - Валидация UUID клиентов
   - Генерация недостающих паролей
   - Конвертация форматов данных
   - Обработка существующих пользователей (обновление)

---

## 🔄 Переключение между типами БД

### Для 3X-UI:

**SQLite → PostgreSQL:**
```env
XUI_DB_TYPE=postgresql
XUI_DB_HOST=localhost
XUI_DB_PORT=5432
XUI_DB_NAME=xui
XUI_DB_USER=postgres
XUI_DB_PASSWORD=your_password
```

**SQLite → MySQL:**
```env
XUI_DB_TYPE=mysql
XUI_DB_HOST=localhost
XUI_DB_PORT=3306
XUI_DB_NAME=xui
XUI_DB_USER=root
XUI_DB_PASSWORD=your_password
```

### Для Pasarguard:

Аналогично, используйте переменные `PASAR_DB_*` вместо `XUI_DB_*`

---

## 📝 Библиотеки

- **python-dotenv** — загрузка переменных окружения из `.env`
- **psycopg2-binary** — драйвер для PostgreSQL
- **pymysql** — драйвер для MySQL/MariaDB

---

## ⚠️ Важные замечания

1. **Резервное копирование**: Перед миграцией обязательно создайте резервную копию обеих баз данных!

2. **Проверка данных**: Скрипт автоматически проверяет наличие администратора в Pasarguard перед миграцией

3. **Перезапуск**: После миграции **обязательно перезапустите Pasarguard** для синхронизации с core config

4. **Существующие пользователи**: Если пользователь с таким email уже существует в Pasarguard, он будет обновлен (не удален!)

5. **Трафик**: Использованный трафик корректно переносится из статистики 3X-UI

---

## 📊 Пример вывода

```
================================================================================
ПОЛНАЯ МИГРАЦИЯ ИЗ 3X-UI В PASARGUARD
================================================================================

[ИНИЦИАЛИЗАЦИЯ] Подключение к базам данных...
  XUI БД: sqlite
  Pasarguard БД: sqlite
  ✓ Подключение успешно!

[ШАГ 1] Извлечение данных из 3X-UI...
  Найдено inbounds: 5
  Найдено записей трафика: 150

[ШАГ 2] Обработка клиентов из inbounds...
  Найдено клиентов: 150

[ШАГ 3] Получение admin_id из Pasarguard...
  Используется admin_id: 1

[ШАГ 4] Получение group_id...
  Используется group_id: 1

[ШАГ 5] Импорт пользователей в Pasarguard...
  Импортирован: user1@example.com (traffic: 52428800)
  Обновлен: user2@example.com
  ...

================================================================================
МИГРАЦИЯ ЗАВЕРШЕНА!
================================================================================
Импортировано новых пользователей: 120
Обновлено существующих пользователей: 30
Пропущено: 0
Ошибок: 0
Всего обработано: 150

ВАЖНО: После миграции перезапустите Pasarguard для синхронизации с core config!
================================================================================
```

---

## ❤️ Dev by witrix

